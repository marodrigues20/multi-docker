apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: web # make a hook with client-deployment.yaml
  ports:
    - port: 3000 # Port will receive the request
      targetPort: 3000 # Where my application is running