apiVersion: v1
kind: Service
metadata:
  name: server-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: web
  ports:
    - port: 5000 # application listen to port 5000
      targetPort: 5000 # Pod will receive connection from port 5000